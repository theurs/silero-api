# Silero TTS: FastAPI Service & Console Utility

Этот репозиторий содержит два мощных инструмента для синтеза русской речи с использованием моделей [Silero Models](https://github.com/snakers4/silero-models) (v5):

1.  **FastAPI Service**: Высокопроизводительный веб-сервис для интеграции синтеза речи в ваши проекты (веб-сайты, боты, бэкенды).
2.  **Console Utility**: Полностью автономная консольная утилита (`silero_cli.py`) для быстрого синтеза речи из текста или SSML прямо из командной строки, без необходимости запускать сервер.

Оба инструмента поддерживают обработку длинных текстов, SSML-разметку и генерируют аудио в форматах `.wav` или `.ogg`.

## 📋 Содержание

-   [Общие предварительные требования](#-общие-предварительные-требования)
-   [Часть 1: FastAPI Service (для разработчиков)](#-часть-1-fastapi-service-для-разработчиков)
    -   [Ключевые особенности сервиса](#-ключевые-особенности-сервиса)
    -   [Установка и запуск сервиса](#-установка-и-запуск-сервиса)
    -   [Использование API](#-использование-api)
-   [Часть 2: Автономная консольная утилита (для пользователей)](#-часть-2-автономная-консольная-утилита-для-пользователей)
    -   [Ключевые особенности утилиты](#-ключевые-особенности-утилиты)
    -   [Использование утилиты](#-использование-утилиты)
    -   [Примеры использования](#-примеры-использования)
    -   [Справка по аргументам командной строки](#-справка-по-аргументам-командной-строки)
-   [Общая информация](#-общая-информация)
    -   [Технологический стек](#-технологический-стек)
    -   [Структура проекта](#-структура-проекта)

---

## 📋 Общие предварительные требования

Перед началом работы убедитесь, что в вашей системе установлены:

1.  **Python 3.10+**
2.  **Git** (для клонирования репозитория)

`FFmpeg` **не является** строгим требованием, так как консольная утилита может скачать его автоматически. Однако для FastAPI-сервиса его рекомендуется установить вручную.

---

## 🚀 Часть 1: FastAPI Service (для разработчиков)

Этот компонент представляет собой готовый к работе асинхронный веб-сервис на FastAPI для синтеза речи по API.

### ✨ Ключевые особенности сервиса

-   **REST API**: Предоставляет эндпоинты для синтеза из простого текста и SSML.
-   **Высокая производительность**: Построен на FastAPI и Uvicorn, идеально подходит для веб-нагрузок.
-   **Потокобезопасность**: Запросы к модели выполняются в очереди, что гарантирует стабильность.
-   **Централизованное логирование**: Все логи пишутся в ротируемый файл `app.log`.
-   **Интерактивная документация**: Swagger UI (`/docs`) и ReDoc (`/redoc`) для удобного тестирования API.

### 🛠️ Установка и запуск сервиса

1.  **Клонируйте репозиторий:**
    ```bash
    git clone https://your-repository-url/silero-tts-project.git
    cd silero-tts-project
    ```

2.  **Создайте и активируйте виртуальное окружение:**
    ```bash
    # Создаем окружение
    python -m venv .venv
    # Активируем (Windows)
    .venv\Scripts\activate
    # Активируем (macOS / Linux)
    source .venv/bin/activate
    ```

3.  **Установите зависимости:**
    ```bash
    pip install -r requirements.txt
    ```

4.  **Запустите сервер:**
    ```bash
    uvicorn app.main:app --host 0.0.0.0 --port 8000 --log-config log_config.yaml
    ```
    -   При первом запуске модель Silero будет скачана (~120 МБ).
    -   Сервер будет доступен по адресу `http://localhost:8000`.

### ⚙️ Использование API

Откройте в браузере `http://localhost:8000/docs` для интерактивного взаимодействия.

-   **Эндпоинт `/tts/text`**: Принимает `POST`-запрос с JSON-телом `{"text": "...", "speaker": "xenia"}`.
-   **Эндпоинт `/tts/ssml`**: Принимает `POST`-запрос с JSON-телом `{"ssml_text": "<speak>...</speak>", "speaker": "xenia"}`.

**Пример с `curl`:**
```bash
curl -X POST "http://localhost:8000/tts/text" \
-H "Content-Type: application/json" \
-d '{"text": "Проверка связи через API."}' \
--output speech.ogg
```

---

## ⌨️ Часть 2: Автономная консольная утилита (для пользователей)

В папке `app/` находится скрипт `silero_cli.py` — мощная утилита командной строки, которая работает **полностью автономно и не требует запущенного сервера**.

### ✨ Ключевые особенности утилиты

-   **Полная автономность**: Не нужен веб-сервер, работает напрямую.
-   **Автоматическая загрузка FFmpeg**: Если `ffmpeg` не найден, утилита предложит скачать его автоматически для конвертации в `.ogg`.
-   **"Умная" нарезка SSML и текста**: Корректно обрабатывает очень длинные тексты и SSML-файлы, разбивая их на безопасные для модели фрагменты и сохраняя контекст разметки.
-   **Плавная склейка**: Добавляет микро-паузы между синтезированными фрагментами, чтобы избежать щелчков и дребезжания на стыках.
-   **Гибкое управление**: Все параметры (голос, текст, пути сохранения) задаются через аргументы командной строки.

### ⚙️ Использование утилиты

**Важно:** Все команды выполняются из корневой папки проекта. Установка зависимостей (`pip install -r requirements.txt`) является общей и для сервиса, и для утилиты.

Базовый формат команды:
```bash
python app/silero_cli.py [ИСТОЧНИК_ТЕКСТА] --save [ПУТЬ_К_ФАЙЛУ] [ОПЦИИ]
```

#### Примеры использования

**1. Синтез из командной строки и сохранение в WAV:**
```bash
python app/silero_cli.py --text "Привет, мир! Это простой тест." --save output.wav
```

**2. Синтез из текстового файла и сохранение в OGG:**
```bash
# Сначала создайте файл in.txt с вашим текстом
python app/silero_cli.py --file in.txt --save speech.ogg
```

**3. Синтез из SSML-файла с выбором другого голоса:**
```bash
# Сначала создайте файл speech.ssml с SSML-разметкой
python app/silero_cli.py --ssml speech.ssml --save result.ogg --speaker eugene
```

**4. Запуск без параметров (покажет справку):**
```bash
python app/silero_cli.py
```

### 📋 Справка по аргументам командной строки

| Аргумент              | Описание                                                                                                   | Обязательный? |
| --------------------- | ---------------------------------------------------------------------------------------------------------- | ------------- |
| `--text "..."`        | Текст для синтеза, передаваемый напрямую в командной строке.                                               | **Да** (один из трёх) |
| `--file "path"`       | Путь к текстовому файлу (`.txt`), содержимое которого будет синтезировано.                                   | **Да** (один из трёх) |
| `--ssml "path"`       | Путь к файлу с SSML-разметкой (`.ssml`, `.xml`).                                                             | **Да** (один из трёх) |
| `--save "path"`       | Путь для сохранения итогового аудиофайла. Расширение (`.wav` или `.ogg`) определяет формат.                 | **Да**          |
| `--speaker "name"`    | Имя голоса. Доступные: `aidar`, `baya`, `kseniya`, `xenia`, `eugene`, `random`. По умолчанию: `xenia`.        | Нет           |
| `--sample_rate rate`  | Частота дискретизации. Доступные: `8000`, `24000`, `48000`. По умолчанию: `48000`.                            | Нет           |

---

## 🌐 Общая информация

### 🛠️ Технологический стек

-   **Синтез речи**: Silero Models v5 (PyTorch)
-   **Веб-фреймворк (API)**: FastAPI & Uvicorn
-   **Обработка текста/SSML**: Razdel, `xml.etree.ElementTree`
-   **Обработка аудио**: NumPy, SciPy
-   **Аудио-конвертация**: FFmpeg (загружается автоматически для утилиты)

### 📂 Структура проекта

```
silero-tts-project/
├── app/
│   ├── __init__.py
│   ├── main.py              # FastAPI-сервер: эндпоинты, логика API
│   ├── tts_model.py         # Модуль-обертка для работы с моделью Silero
│   ├── text_processor.py    # (Для API) Нарезка длинных текстов
│   └── silero_cli.py        # Автономная консольная утилита (не связана с API)
│
├── .venv/                   # Виртуальное окружение
├── app.log                  # Файл логов для FastAPI-сервиса
├── log_config.yaml          # Конфигурация логирования для Uvicorn
├── requirements.txt         # Общий список зависимостей Python
└── README.md                # Этот файл
```
